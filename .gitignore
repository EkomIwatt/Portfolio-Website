# build output
dist/
# generated types & cache
.astro/
# dependencies
node_modules/
# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# environment variables
.env
.env.production
# macOS / editor
.DS_Store
*.local

# Claude Code per-user local settings (permissions etc.) — never commit
.claude/settings.local.json

# Third-party reference material — kept locally, NOT republished to this public repo
# (wibify.agency screenshots, copyrighted checklist PDF). The authored spec/notes
# (.claude/inspiration/wibify.md, components/) and Ekom's own pictures ARE committed.
.claude/inspiration/assets/
.claude/rules/
